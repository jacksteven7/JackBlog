<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @post.title %>
</p>

<p>	<% if @post.has_photo? %>
		<%= image_tag @post.photo_path %>
	<% else %>
		<p>Problem listing your pic</p>
	<% end %>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
